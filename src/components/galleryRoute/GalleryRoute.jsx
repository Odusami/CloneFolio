import React from "react";

import { Outlet, Route, Routes } from "react-router-dom";

const GalleryRoute = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};


export default GalleryRoute;
